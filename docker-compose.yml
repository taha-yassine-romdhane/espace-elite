version: '3.8'

services:
  # Next.js application
  espace-elite-app:
    build: 
      context: .
      dockerfile: Dockerfile
    container_name: espace-elite-app
    restart: always
    ports:
      - "3001:3001"
    env_file:
      - .env
    environment:
      - NODE_ENV=production
      - PORT=3001
      # Update this to point to your existing database on the VPS
      # Format: postgresql://username:password@host:port/database_name
      - DATABASE_URL=${DATABASE_URL:-postgresql://postgres:${POSTGRES_PASSWORD}@localhost:5432/espace_elite}
      - NEXTAUTH_URL=https://assistance.elitemedicaleservices.tn
      - NEXTAUTH_SECRET=${NEXTAUTH_SECRET:-elitemedicaleservices_secret_key}
    # Using host network to easily connect to the local database on the VPS
    network_mode: "host"

# No volumes needed since we're not running the database in Docker

# No custom networks needed with host network mode
